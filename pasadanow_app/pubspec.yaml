name: pasadanow
description: Tricycle ride-hailing

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.0
  go_router: ^13.0.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  geolocator: ^14.0.2
  http: ^1.2.1          # ← OSRM routing API calls

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/